#debug
option /etc/ppp/options.pptpd
speed 115200
stimeout 10
# localip & remoteip are not needed, ip management is done by pppd (by ./configure "--with-pppd-ip-alloc").
# but we need localip & remoteip for pptpd to assign ip addresses due to the max concurrent connection limit. 
